--- core.js
+++ core.js
@@ -137,6 +137,8 @@
 let myStats = { games: 0, wpm: 0, wins: 0 };
 let myAchievements = [];
 let myInventory = [];
+let myBio = "";
+let mySocialLink = "";
 let myJoined = 0;
 let myItemToggles = {};
 let builderInventory = null;
@@ -165,6 +167,8 @@
       if (prop === "adminSettings") return adminSettings;
       if (prop === "itemToggles") return myItemToggles;
       if (prop === "inventory") return myInventory;
+      if (prop === "bio") return myBio;
+      if (prop === "socialLink") return mySocialLink;
       return undefined;
     },
     set(target, prop, value) {
@@ -216,6 +220,12 @@
       if (prop === "inventory") {
         myInventory = value;
         return true;
+      }
+      if (prop === "bio") {
+        myBio = value;
+        return true;
+      }
+      if (prop === "socialLink") {
+        mySocialLink = value;
+        return true;
       }
       return false;
     }
@@ -3438,6 +3448,8 @@
   myStats = data.stats || { games: 0, wpm: 0, wins: 0 };
   myAchievements = data.achievements || [];
   myInventory = data.inventory || [];
+  myBio = data.bio || "";
+  mySocialLink = data.socialLink || "";
   myJoined = data.joined || 0;
   builderInventory = data.builderInventory || null;
   builderHotbar = data.builderHotbar || null;
@@ -3606,6 +3618,8 @@
     builderInventory: null,
     builderHotbar: null,
     builderArmor: null,
+    bio: "",
+    socialLink: "",
   };

   const localProfile = getLocalProfile(normalized);
