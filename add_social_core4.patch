--- core.js
+++ core.js
@@ -3200,6 +3200,7 @@
   "overlayProfile",
   "overlaySeason",
   "overlayCrew",
+  "overlaySocial",
   "overlayAdmin",
   "overlayGamebox",
 ];
@@ -3212,6 +3213,9 @@
   if (id === "overlaySeason") renderSeasonPanel();
   if (id === "overlayCrew") renderCrewPanel();
   if (id === "overlayAdmin" && state.myName === "ANON") return;
+  if (id === "overlaySocial" && typeof window.loadSocialFeed === "function") {
+    window.loadSocialFeed();
+  }

   const active = document.querySelector(".overlay.active");
   if (active && active.id !== id && active.id !== "overlayLogin") {
