1. Add profile fields in HTML (bio and social media app): `index.html`. We already applied this partially, but we should make sure the input and display elements are there.
2. In `core.js`: Add variables `myBio` and `mySocialLink` locally.
3. Expose them through the proxy state in `core.js`.
4. Update `saveStats()` to include the bio and socialLink.
5. Update `loadProfile()` to load `bio` and `socialLink` and set the HTML elements correctly.
6. Update `script.js` to attach a click listener to save the profile info.
7. Pre commit steps.
8. Submit.
